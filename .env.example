# Copy this file to .env and replace the placeholder values.
OPENAI_API_KEY=sk-your-openai-key
LLM_PROVIDER=ollama
LLM_MODEL=
# Uncomment to force embeddings onto a specific torch device (auto, cuda, cuda:0, mps, cpu)
# EMBEDDING_DEVICE=auto
